services:
  superset:
    image: apache/superset:latest
    container_name: superset
    restart: always

    environment:
      SUPERSET_ENV: production
      SUPERSET_LOAD_EXAMPLES: "no"

      # Datos del usuario admin
      ADMIN_USERNAME: admin
      ADMIN_PASSWORD: admin12345
      ADMIN_FIRST_NAME: Superset
      ADMIN_LAST_NAME: User
      ADMIN_EMAIL: danmiranda2011@gmail.com

      # CONEXIÓN CORRECTA A POSTGRES
      SQLALCHEMY_DATABASE_URI: postgresql+psycopg2://postgres:postgres12345@postgres:5432/postgresdb

      # SECRET KEY OBLIGATORIO
      SECRET_KEY: "kJ3s9D+Zp0n3u8LwA1d7F4bX9qQ2s5!v"

    ports:
      - "8088:8088"

    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:8088/health"]
      interval: 10s
      timeout: 10s
      retries: 10

networks:
  default:
    external: true
    name: airflow_network